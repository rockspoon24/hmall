spring:
  application:
    name: gateway
  profiles:
    active: dev
  cloud:
    nacos:
      server-addr: 8.134.144.160:8848
      config:
        file-extension: yaml
        shared-configs:
          - data-id: shared-log.yaml